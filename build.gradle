group 'a123'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.8

tasks.withType(JavaCompile) {
    options.encoding = 'UTF-8'
}

repositories {
    mavenCentral()
}

dependencies {
    compile files('libs/tools.jar')
}

apply plugin: 'application'

mainClassName = 'sample.JDIMain'

